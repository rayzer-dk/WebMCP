name: Build competition OCMOD

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'opencart-module/**'
      - '.github/workflows/build-dist.yml'
  issues:
    types: [opened]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build package
        shell: bash
        run: |
          set -euo pipefail
          rm -f dist/WebMCP_Catalog_Challenge_v1.0.0.ocmod.zip
          cd opencart-module
          zip -qr -9 ../dist/WebMCP_Catalog_Challenge_v1.0.0.ocmod.zip install.xml upload
          cd ..
          unzip -t dist/WebMCP_Catalog_Challenge_v1.0.0.ocmod.zip
          sha256sum dist/WebMCP_Catalog_Challenge_v1.0.0.ocmod.zip | sed 's#dist/##' > dist/SHA256SUMS.txt

      - name: Commit synchronized package
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dist/WebMCP_Catalog_Challenge_v1.0.0.ocmod.zip dist/SHA256SUMS.txt
          if git diff --cached --quiet; then
            echo 'Package already synchronized.'
            exit 0
          fi
          git commit -m 'Build audited competition OCMOD package'
          git push
